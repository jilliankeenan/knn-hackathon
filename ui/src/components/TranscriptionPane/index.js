import React from 'react';
import { Wrapper } from './styled'

const TranscriptionPane = ({ text }) => {
    return (
        <Wrapper>
            {text}
        </Wrapper>
    );
}

export default TranscriptionPane;